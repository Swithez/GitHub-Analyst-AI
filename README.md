# 📊 GitHub Analytics Microservices

Интеллектуальная система мониторинга и анализа репозиториев GitHub. Проект построен на микросервисной архитектуре, использует AI для генерации отчетов и поддерживает уведомления через Telegram.

## 🚀 Быстрый старт (Docker)

Для запуска всей системы вам потребуется установленный **Docker** и **Docker Compose**.

### 1. Подготовка окружения

Создайте файл `.env` в корневой директории проекта и заполните его вашими ключами:

```env
# GitHub
# Получить здесь: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# Mistral AI 
# Получить здесь: https://console.mistral.ai/
MISTRAL_API_KEY=your_mistral_key_here

# Telegram
# Получить у @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Services URLs (внутри сети Docker)
API_GATEWAY_URL=http://api-gateway:8000
GITHUB_SERVICE_URL=http://github-service:8001
ANALYTICS_SERVICE_URL=http://analytics-service:8002
DATABASE_SERVICE_URL=http://database-service:8003

```

### 2. Запуск

Выполните команду в терминале:

```bash
docker-compose up --build

```

После завершения сборки веб-интерфейс будет доступен по адресу: **http://localhost:8080**

---

## 🏗 О проекте

### Архитектура микросервисов

Система разделена на независимые модули, что позволяет легко обновлять и масштабировать каждый компонент:

* **Web Client (Port 8080):** Интерфейс для взаимодействия с пользователем (FastAPI + Jinja2).
* **API Gateway (Port 8000):** Единая точка входа, координирующая работу всех внутренних сервисов.
* **GitHub Service (Port 8001):** Модуль сбора данных (коммиты, PR, Issues, языки) через GitHub REST API.
* **Analytics Service (Port 8002):** Генерация выводов и рекомендаций с использованием LLM **Mistral AI**.
* **Database Service (Port 8003):** Хранение истории запросов и накопленной статистики (SQLite/PostgreSQL).

### Основной функционал

1. **Сбор данных за период:** Анализ активности репозитория в выбранном диапазоне дат.
2. **AI Рекомендации:** Получение советов по улучшению кода и управления проектом на основе реальных данных.
3. **Визуализация:** Наглядное отображение активности контрибьюторов и используемых технологий.
4. **История:** Быстрый доступ к результатам предыдущих проверок.

## 🛠 Технологии

* **Backend:** FastAPI (Python 3.10+)
* **AI:** Mistral AI API
* **Data:** Requests, Pydantic
* **Infrastructure:** Docker, Docker Compose
* **Frontend:** HTML/CSS (Jinja2 Templates)
